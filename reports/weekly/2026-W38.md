# Agent Memory 周报：2026-W38

> 部分周覆盖：监测于 2026-09-16 06:31:26 UTC 开始，本周不是完整七日样本。以下仅总结已经取得一手证据的变化，不把首次读到的历史发布当作本周新事件。

## 本周范围

- 监测对象从首轮 8 个 tracked 项目 + 1 个历史参照，逐步扩展到 20 个 tracked 项目 + 1 个历史参照；扩容造成的样本变化不用于伪造同口径增长排名。
- 每轮采集 GitHub repository metadata、默认分支 HEAD 与当前 Release 水位；Release 历史未做穷尽式回灌。
- broad discovery 与无 Star 门槛的新项目通道均执行；部分 broad 查询在页预算结束时仍有满页结果，因此不能声称覆盖整个 GitHub 生态。
- 监测历史尚不足 7 天，本周不发布 7/30 日 Star 增长结论。

## 实质进展

### Coding Agent / 工程接入

- Hindsight：Coding Agent 的记忆注入开始优先检索 curated knowledge pages，在信息不足时再使用更重的 reflect；这是从“直接把历史塞进上下文”向“先检索结构化知识、再按需综合”的工程化路径。
- OpenViking：加入 Experience Memory skill，并在 v0.4.21 继续扩展 Hermes、MiMo/MiMoCode、WorkBuddy 等 Agent/Memory 接入，同时修复检索、MCP 与部署可靠性问题。
- TencentDB Agent Memory：修复 Claude Code 2.1.277+ 的 Anthropic 协议兼容问题。适配器现在区分真正的 top-level system 与 messages 中的 role:system，避免记忆注入重写 system 后静默丢失 AGENTS.md 指令。
- Letta Code：v0.32.13 / v0.32.14 持续收敛 Coding Agent 的 memory/reflection runtime，包括 process-tree 与 gateway 恢复、memory filesystem sync，以及 /remember、/dream、/reflect 和 legacy memory-tool 路径的整理。

### 记忆层 / 本地部署

- Cognee v1.6.0 强化 keyless/local-model 工作流、pipeline recovery、dataset-scoped recall，并记录 dataset 对应 embedding model；这些结论来自正式 Release notes，未独立执行上游代码。
- TencentDB Agent Memory v2.0.2-beta.2 增加 MongoDB 存储及本地部署相关能力，并修复 recall/解析/历史消息等问题；按 upstream statement 记录。

### 评测

- 继续跟踪 LongMemEval、LongMemEval-V2 与 agent-memory-benchmark 等评测资产。
- discovery 中出现 MemoryAgentBench、mem0ai/memory-benchmarks 等值得后续审查的 benchmark 线索；本周未把 README 中的分数当作独立复现结果。

## 本周判断

多个独立项目正在把 Agent Memory 从单纯“历史召回”推进到更明确的任务前检索、结构化经验/知识页、作用域控制和运行时可靠性。与此同时，Coding Agent 场景开始暴露一个更具体的系统问题：memory middleware 不只是要“召回正确”，还必须在代理协议转换、系统指令、工具调用和反思流程之间保持上下文语义不丢失。

## 覆盖限制

- 这是监测开始所在周的部分周，不代表完整 7 天。
- 现有历史 Release 没有全量回灌；当前 Release 主要用作 forward watermark。
- broad discovery 非穷尽；查询结果条数不能解释为整个方向的新项目数量。
- 没有执行被跟踪项目代码，没有复现 benchmark；Release/README 结论仍按 upstream_statement 与 code_inspected 分级。
- 7/30 日窗口尚无合格基线，因此不发布增长榜。
